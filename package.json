{
  "name": "megamarket",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && nest build",
    "start": "cd backend && npx prisma migrate deploy && npx prisma db seed && node dist/main.js"
  }
}